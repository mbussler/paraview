CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

FIND_PACKAGE(CUDA REQUIRED)

CUDA_INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

CUDA_ADD_LIBRARY(cudaRidge
	SHARED
	cudaRidge.cu
	OPTIONS -arch=sm_20 --pre-include cudaRidge.h
)
